<%= form_for(@gps_point) do |f| %>
  <% if @gps_point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gps_point.errors.count, "error") %> prohibited this gps_point from being saved:</h2>

      <ul>
      <% @gps_point.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :latitude %><br>
    <%= f.text_field :latitude %>
  </div>
  <div class="field">
    <%= f.label :longitude %><br>
    <%= f.text_field :longitude %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
